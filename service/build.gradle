apply plugin: 'war'

dependencies {
    providedCompile group: 'io.swagger', name: 'swagger-annotations', version: '1.5.0'
    compile group: 'org.postgresql', name: 'postgresql', version: '42.2.8'
    compile project(':core')
}